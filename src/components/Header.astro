---
import Logo from "../components/Logo.astro";
import Search from "../components/SearchBar.astro";

const { pagePath } = Astro.props;

---

<header>
    <Logo />
    <Search pagePath={pagePath} />
</header>